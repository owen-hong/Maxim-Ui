#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../svg-sprite/bin/svg-sprite.js" "$@"
  ret=$?
else 
  node  "$basedir/../svg-sprite/bin/svg-sprite.js" "$@"
  ret=$?
fi
exit $ret
